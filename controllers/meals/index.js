const createMeal = require("./createMeal");
const updateMeal = require("./updateMeal");
const deleteMeal = require("./deleteMeal");
const getMealsByDate = require("./getMealsByDate");

module.exports = {
  createMeal,
  updateMeal,
  deleteMeal,
  getMealsByDate,
};
